import { BaseContractMethod, ITransactionRequestConfig, Logger } from "@maticnetwork/maticjs";
import { Contract } from "ethers";
import { TransactionWriteResult } from "../helpers";
export declare class ContractMethod extends BaseContractMethod {
    private contract_;
    private methodName;
    private args;
    constructor(logger: Logger, contract_: Contract, methodName: any, args: any);
    get address(): string;
    toBigNumber(value: any): any;
    private toConfig_;
    encodeABI(): string;
    estimateGas(config?: ITransactionRequestConfig): Promise<number>;
    read(config: ITransactionRequestConfig): any;
    private getMethod_;
    write(config: ITransactionRequestConfig): TransactionWriteResult;
}
