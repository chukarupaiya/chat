import { ITransactionWriteResult } from "@maticnetwork/maticjs";
import { providers } from "ethers";
export declare class TransactionWriteResult implements ITransactionWriteResult {
    private promise;
    onTransactionHash: any;
    onTransactionError: any;
    onTransactionReceipt: Function;
    getReceipt(): Promise<import("@maticnetwork/maticjs").ITransactionReceipt>;
    constructor(promise: Promise<providers.TransactionResponse>);
    getTransactionHash(): Promise<string>;
}
