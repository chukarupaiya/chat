import { ERC20 } from "./erc20";
import { HermezBridgeClient } from "../utils";
import { IHermezClientConfig } from "../interfaces";
export * from "./hermez_bridge";
export * from "./bridge_util";
export declare class HermezClient extends HermezBridgeClient<IHermezClientConfig> {
    init(config: IHermezClientConfig): Promise<this>;
    /**
     * creates instance of ERC20 token
     *
     * @param {string} tokenAddress
     * @param {boolean} isParent
     *
     * @returns
     * @memberof ERC20
     */
    erc20(tokenAddress: string, isParent?: boolean): ERC20;
    private getContracts_;
}
