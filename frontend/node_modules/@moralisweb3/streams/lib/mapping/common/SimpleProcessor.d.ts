import { Block, IWebhook } from '@moralisweb3/streams-typings';
import { Document, Update } from '../storage';
import { CollectionNameBuilder } from './CollectionNameBuilder';
export declare class SimpleProcessor<Doc extends Document, Item> {
    private readonly collectionNameBuilder;
    private readonly documentBuilder;
    constructor(collectionNameBuilder: CollectionNameBuilder, documentBuilder: (item: Item, block: Block, confirmed: boolean, chainId: string) => Doc);
    process(items: Item[], batch: IWebhook): Update<Doc>[];
}
//# sourceMappingURL=SimpleProcessor.d.ts.map